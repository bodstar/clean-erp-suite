

## Plan: Code Batches for M-Promo Codes Page

### Summary
Replace the flat promo codes table with a batch-first view. Each generate call creates a batch; the UI lists batches as expandable cards with inline code tables, plus PDF coupon and CSV export per batch.

### Changes

#### 1. API layer (`src/lib/api/mpromo.ts`)
- Add `CodeBatch` interface export
- Add `getCodeBatches(params, scope)` and `getCodeBatch(id)` functions
- Update `generateCodes` return type to `{ count: number; batch_id: number }`
- In DEMO_MODE path for `generateCodes`, return `{ count: data.quantity, batch_id: Date.now() }`
- Add DEMO_MODE paths for `getCodeBatches` and `getCodeBatch` that synthesize batches from existing `demoCodes` grouped by `campaign_id` + `expires_at`

#### 2. Types (`src/types/mpromo.ts`)
- Export `CodeBatch` type (or keep it in the API file since it's API-specific — will co-locate with the API functions for simplicity)

#### 3. Redesign `src/pages/mpromo/MPromoCodes.tsx`
**Generate panel** — keep as-is at top. After successful generate, use returned `batch_id` to fetch `getCodeBatch(batch_id)` and prepend to batch list.

**Batch list** — replace DataTable with a list of Card components, each showing:
- Campaign name, created_at date, generated_by_name
- Quantity, redemption amount (GH₵), expiry
- Progress indicator: `{redeemed_count} redeemed / {active_count} active / {quantity} total`
- "View Codes" toggle button and "Export" dropdown (PDF / Excel)
- Team badge when `scopeMode === "all"`

**Expanded codes table** — when "View Codes" is clicked, expand inline with a simple table: Code, Status, Issued To, Redeemed At. No pagination.

**PDF export** — on "Download PDF":
1. Fetch full batch via `getCodeBatch(id)` to get `codes[]`
2. Open a new window with a styled HTML document containing a coupon grid
3. 2 columns × 4 rows = 8 coupons per A4 page, dashed borders
4. Each coupon: "Mobile Water" header, campaign name, large mono code, reward amount, expiry, "Dial *920*7# to redeem", batch reference
5. Auto-trigger `window.print()`

**CSV export** — on "Download Excel":
1. Fetch full batch codes
2. Build CSV string with headers: Batch ID, Campaign, Code, Status, Redemption Amount (GH₵), Expires At, Generated By, Generated At
3. Create blob data URL and trigger download as `batch-{id}-codes.csv`

#### 4. Pagination for batch list
- Keep page/search state for batch listing using `getCodeBatches` with pagination params
- Search filters by campaign name

### Files Modified
1. `src/lib/api/mpromo.ts` — new functions + updated generateCodes
2. `src/types/mpromo.ts` — add CodeBatch type  
3. `src/pages/mpromo/MPromoCodes.tsx` — full redesign

### Permissions
- All existing permission checks (`canGenerate`, `canCancel`) preserved
- Cancel button moves into the expanded codes table per-code row

